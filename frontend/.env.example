# Local Django (`npm run dev` reads .env.development).
VITE_API_URL_LOCAL=http://127.0.0.1:8000/api/v1

# Railway Django (`npm run build` reads .env.production).
# VITE_API_URL_PRODUCTION=https://pulsif-production.up.railway.app/api/v1

VITE_LENIS=false
# Optional. Checkout also receives the publishable key from the API.
# VITE_STRIPE_PK=
